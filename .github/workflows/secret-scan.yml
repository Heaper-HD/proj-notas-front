name: Secret scan
on:
    pull_request:
    push:

jobs:
    gitleaks:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
              with:
                  fetch-depth: 0
            - uses: gacts/gitleaks@v1
